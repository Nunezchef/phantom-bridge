interface:
  display_name: "Phantom Bridge"
  short_description: "Repo-specific patterns and workflows for phantom-bridge"
  default_prompt: "Use the phantom-bridge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true